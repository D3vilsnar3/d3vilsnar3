name = "d3vilsnar3"
compatibility_date = "2026-09-06"

# Static-asset-only Worker: no server code, Cloudflare just serves the
# contents of public/ (the built portfolio site) from its edge network.
[assets]
directory = "./public"
